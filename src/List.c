#include"List.h"

List* createList()
{
  List* newlist = (List*) malloc(sizeof(List));
  newlist->data = NULL;
  newlist->next = NULL;
  newlist->prev = NULL;
  return newlist;
}

size_t lenList
(List *s)
{
  size_t len;
  return len;
}
