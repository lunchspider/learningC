#include <stdlib.h>
#include<stdio.h>


#ifndef SORT_H
#define SORT_H

void insert_sort(int *, size_t );

void insert_sort_reverse(int *, size_t);

void print_arr(int *, size_t );

void scan_arr(int *, size_t);

void swap(int *, int *);
#endif
